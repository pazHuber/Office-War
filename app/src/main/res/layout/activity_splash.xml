<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.paz.officewar.SplashActivity"
    android:background="@drawable/main_background">

    <ProgressBar
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/act_splash_progress_bar"
        android:maxWidth="200dp"
        android:minHeight="30dp"
        android:minWidth="200dp"
        android:progressDrawable="@drawable/greenprogress"
        android:progress="50"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="94dp" />

    <TextView
        android:id="@+id/act_splash_presentage_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/act_splash_progress_bar_txt"
        android:textSize="25dp"
        android:textColor="#ffffff"
        android:layout_alignRight="@+id/act_splash_progress_bar"
        android:layout_alignEnd="@+id/act_splash_progress_bar" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/act_splash_msg_to_user_text"
        android:layout_below="@+id/act_splash_progress_bar"
        android:layout_alignLeft="@+id/act_splash_progress_bar"
        android:layout_alignStart="@+id/act_splash_progress_bar"
        android:text="@string/act_splash_msg_to_user_txt"
        android:textSize="35dp"
        android:layout_marginTop="186dp"
        android:fontFamily="serif"
        android:textColor="#ffffff"
        android:textStyle="bold" />

</RelativeLayout>
